# Agent Handoffs

Place temporary handoff notes here when work is split between agents.

Use the template from `docs/agents/AGENT_HANDOFF.md`. Commit permanent decisions into
`AGENTS.md`, `DESIGN_SYSTEM.md`, or `docs/agents/AGENT_DESIGN_CONTRACT.md` instead of
leaving them only in this folder.
