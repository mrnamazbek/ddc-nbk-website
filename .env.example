# This public website has no runtime secret environment variables.
#
# The variables below are optional local-only credentials for approved asset
# discovery tools. They are not read by the deployed application and must never
# be exposed through NEXT_PUBLIC_* values or committed to Git.
# Never place production credentials in this repository.

MOTION_TOKEN=
ICONSCOUT_CLIENT_ID=
ICONSCOUT_CLIENT_SECRET=
ICONS8_API_KEY=
